{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the main window. Only third-party plugin commands need explicit ACL grants here; in-app #[tauri::command] handlers are exposed through invoke_handler! without entries.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
