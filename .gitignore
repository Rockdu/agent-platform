# Build artifacts
target/
dist/
build/
node_modules/

# Tauri build output
src-tauri/target/

# Codegen output (regenerated each build per AC-1.2)
src-tauri/src/generated/
src/generated/

# Logs
*.log

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# Per-machine Claude Code settings (don't share between machines)
.claude/settings.local.json

# Humanize local loop state (enforced by humanize plugin hook policy)
# Plan, ideas, round artifacts, bitlesson all live here as local-only files.
.humanize/

# Workspaces live OUTSIDE this repo at ~/AgentPlatform/workspaces/
# (Listed here for clarity even though they're not in the repo tree.)
