# Build artifacts
target/
dist/
build/
node_modules/

# Tauri build output
src-tauri/target/

# Codegen output (regenerated each build per AC-1.2)
src-tauri/src/generated/
src/generated/

# Logs
*.log

# TypeScript incremental + Vite config compiled output (built by `tsc -b`)
*.tsbuildinfo
vite.config.d.ts
vite.config.js

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# Per-machine Claude Code settings (don't share between machines)
.claude/settings.local.json

# Humanize local loop state (user policy reversal 2026-05-16: complied
# with plugin hook after the conflict made the loop unworkable).
.humanize/

# Workspaces live OUTSIDE this repo at ~/AgentPlatform/workspaces/.
